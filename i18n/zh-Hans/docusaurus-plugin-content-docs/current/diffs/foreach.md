---
sidebar_position: 10
title: foreach 在哪里？
---

# foreach 在哪里？

在 **hapet** 编程语言中，没有 ***foreach*** 关键字。只需使用 ***for*** 循环：  
```csharp
string str = "Hello World!";
for (char c in str)
{
    Console.Write(c);
}
```

为什么这样做？只是因为 :)
